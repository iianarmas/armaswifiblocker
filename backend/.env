# Armas WiFi Control API Configuration

# Secret API key for authentication (change this to a random string)
API_KEY=change-this-to-a-secure-random-key

# Network configuration
NETWORK_RANGE=192.168.100.0/24
NETWORK_INTERFACE=eth0

# Server settings
HOST=0.0.0.0
PORT=5000
DEBUG=false

# CORS - allowed origins (comma-separated)
CORS_ORIGINS=*
